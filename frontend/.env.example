VITE_API_URL=http://localhost:5000/api
# Off by default. Only set to "true" in local/demo builds — it ships 8
# hardcoded demo DNIs + a shared password in the built bundle.
VITE_ENABLE_DEMO_LOGIN=false
